import { theme } from "@chakra-ui/react";

export default fonts;
export const fonts = {
  heading: `"InterVariable", "Inter", ${theme.fonts.heading}`,
  body: `"InterVariable", "Inter", ${theme.fonts.body}`,
  mono: `"JetBrains MonoVariable", "JetBrains Mono", ${theme.fonts.mono}`,
};
